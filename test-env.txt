- Debian
- Packages:
	```bash
	sudo apt-get install \
		bash-completion \
		git \
		vim \
		python3 \
		python3-pip \
		python3-pygame \
		openssh-server \
		xorg
	```
- Python modules:
	- `sudo python3 -m pip install pytmx pyscroll`
- Git project:
	```bash
	sudo mkdir /usr/local/bin/robot
	sudo chown pi:pi /usr/local/bin/robot
	git clone https://github.com/synadrin/robot.git /usr/local/bin/robot
	```
- Xorg:
	- `#echo "pgrep 'tmux|startx' || startx" >> ~/.profile`
	- ~/.xinitrc:
		```
		#!/bin/sh

		exec /usr/local/bin/robot/run.sh
		```
- Auto-login:
	- `sudoedit /lib/systemd/system/getty@.service`
	- Change  
	`ExecStart=-/sbin/agetty --noclear %I $TERM`  
	to `ExecStart=-/sbin/agetty --noclear -a pi %I $TERM`
